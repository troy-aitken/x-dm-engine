{
  "name": "x-dm-engine",
  "version": "0.2.0",
  "private": true,
  "description": "Auto-DM users who like or reply to a tweet, with UTM tracking and dedup. Self-hosted, BYO X API keys.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=@x-dm-engine/api",
    "dev:web": "npm run dev --workspace=@x-dm-engine/web",
    "build": "npm run build --workspace=@x-dm-engine/api && npm run build --workspace=@x-dm-engine/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "oauth": "npm run oauth --workspace=@x-dm-engine/api --",
    "setup-notion": "npm run setup-notion --workspace=@x-dm-engine/api"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
